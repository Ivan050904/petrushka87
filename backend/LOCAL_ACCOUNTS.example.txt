# Локальные аккаунты для разработки (не для продакшена).

petr@petr.local / petr12345
demo@folio-one.local / demo12345

# После git pull на новой машине:
# 1. cd backend && pip install -e ".[dev]"
# 2. DATABASE_URL=sqlite:///./storage/folio_one.db в .env
# 3. python scripts/bootstrap_data.py
# 4. start-dev.bat

# Одна база: backend/storage/folio_one.db (не коммитится в git).
